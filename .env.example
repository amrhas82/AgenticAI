########################################
# Environment variables for AI Playground
########################################

# OpenAI (optional; only needed if using OpenAI provider)
OPENAI_API_KEY=

# Ollama
# If running outside Docker: http://localhost:11434
# If running in Docker on Linux: http://host.docker.internal:11434
OLLAMA_HOST=http://localhost:11434

# Embeddings
EMBED_MODEL=nomic-embed-text
EMBED_DIM=768

# Vector store fallback JSON path (used when DATABASE_URL is unset or psycopg2 unavailable)
VECTOR_JSON_PATH=data/memory/vector_store.json

# Database (optional; enables Postgres + pgvector path)
# Example for local compose: postgresql://ai_user:ai_password@postgres:5432/ai_playground
DATABASE_URL=

# Model Context Protocol (optional)
MCP_URL=http://localhost:8080

# Example environment configuration
# Copy to .env and adjust as needed

# OLLAMA: Host where the Ollama server runs (containers use host.docker.internal)
OLLAMA_HOST=http://localhost:11434

# Embedding model and dimension (used by VectorDB)
EMBED_MODEL=nomic-embed-text
EMBED_DIM=768

# MCP client endpoint (optional)
MCP_URL=http://localhost:8080

# Optional OpenAI API key (can also be provided in UI)
# OPENAI_API_KEY=
